// List of origins that are allowed to make cross-origins requests
// the server 
// LOCAL DEVELOPMENT ORIGIN & PRODUCTION DOMAIN

const allowedOrigins = [
    'http://localhost:3000',
    'https://davidliberal.com', 
]

module.exports = allowedOrigins